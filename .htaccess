<IfModule mod_rewrite.c>
  Options +FollowSymlinks -Multiviews
  RewriteEngine On
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} !-f
  # Rules for main layouts
    RewriteRule ^proof/([a-zA-Z0-9\-]+)$ /proof.php?file=$1 [QSA,L]
</IfModule>
